syntax = "proto3";
package healthcheck.types;

option go_package = "healthcheck/x/types";

message HandshakeMetadata {
  
  string version = 1; 
  uint32 updateInterval = 2; 
  uint32 timeoutInterval = 3; 
}
